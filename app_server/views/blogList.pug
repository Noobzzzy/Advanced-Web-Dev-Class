extends layout

block content

  h1(style = {color = blue}) List of Blogs

    .row.list-group
      each blog in blogs
        .col-xs-12.list-group-item
          h3 blog.title
          p1 blog.text
          p4 blog.timestamp


